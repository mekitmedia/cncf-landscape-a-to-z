# CNCF Landscape A-to-Z Environment Variables
# Copy this file to .env or load directly with 1Password CLI

# Option 1: 1Password CLI Secret References (used with `op run` or `op inject`)
GOOGLE_API_KEY="op://<vault>/<item>/<field>"
# PYDANTIC_AI_GATEWAY_API_KEY="op://<vault>/<item>/<field>"

# Option 2: Direct API Keys (Uncomment if setting keys directly in local .env)
# GOOGLE_API_KEY="your_google_api_key_here"
# PYDANTIC_AI_GATEWAY_API_KEY="your_gateway_key_here"

# Model Configuration
GEMINI_MODEL="gateway/google-vertex:gemini-2.5-flash"

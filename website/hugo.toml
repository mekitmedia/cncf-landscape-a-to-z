baseURL = 'https://mekitmedia.github.io/cncf-landscape-a-to-z/'
languageCode = 'en-us'
title = 'CNCF Landscape Overview'
theme = 'cncf-theme'
buildFuture = true
dataDir = "../data"
ignoreFiles = ['README\.md$']

[params]
  description = "A 52-week journey through the CNCF Landscape, exploring one letter every two weeks."
  author = "CNCF Journey Team"
  images = ["/images/og-image.png"] # Note: Ensure this exists or is replaced

[caches]
  [caches.images]
    dir = ':cacheDir/images'
